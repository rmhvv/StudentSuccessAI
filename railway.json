{
  "$schema": "https://railway.app/json-schemas/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'python -m uvicorn main:app --host 0.0.0.0 --port $PORT & python -m streamlit run app.py --server.port 8501 --server.address 0.0.0.0 --server.headless=true'",
    "restartPolicyType": "ON_FAILURE"
  }
}